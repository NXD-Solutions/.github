# Compression signals understanding

## 2026-04-12 — New principle under Pattern-seeking by Instinct

**Trigger:** Peter's repeated observation across sessions — "Again this is an example of problems better understood, collapses the implementation." The word "Again" triggered formal examination via an 8-question Socratic inquiry (`SM: Implementation collapse as a DNA quality`).

**Session chain:** PR #43 (Manager-Assistant-v2) and PR #41 (NXD-Solutions/packages) review → `config.ts` defect propagation (fixed in one artefact, survived in sibling) → self-maintain scan → pattern candidate "find all instances of a root cause" → IFD lifted it to "Find the pattern" scope clause issue (PR #319) → self-maintain gate surfaced Peter's recurring observation → SM inquiry → this principle.

**Key SM turning points:**
- Peter: "it should sip through everything we do, as a quality statement or ambition, as a direction of evolving" — established the principle is pervasive, not local
- Peter: "at least DNA and code, anywhere where reuse is mandated" — scoped the principle's domain
- Peter's doubt: "Is IFD one implementation of this?" — the turning point that established the principle sits above IFD, not alongside it. IFD is one path to compression; the principle names the signal regardless of path.

**IFD analysis:** (1) the observation is more fundamental than IFD; (2) IFD is one implementation path, not the only one; (3) no existing principle names compression as the quality signal of understanding regardless of mechanism.

**Decision:** Add a new principle "Compression signals understanding" as a sibling to "Find the pattern, not the instance" under Pattern-seeking by Instinct. The principle names compression as a quality signal of deepened understanding, mechanism-agnostic — pattern-seeking, experience, deliberation, failed implementation, and evidence arrival all qualify as paths.

**Ruled out:**
- *Lifecycle dimension (placement, deferral, promotion):* Challenged during deliberation — compression is epistemological; lifecycle is operational. Bundling produces a principle doing two things. The lifecycle dimension is already covered: "Start lean, don't close the door" handles honest placement; "What holds for one holds for many" and "Build once for many" handle promotion signals. Stripped.
- *Universal claim (understanding always compresses):* Challenged — understanding can reveal the problem is larger than assumed (honest scope expansion). Compression framed as quality signal, not guaranteed outcome.
- *Expanding "Find the pattern":* The gap is not that "Find the pattern" is incomplete — it correctly names pattern-seeking → compression. The gap is that compression is attributed to one mechanism when it belongs to a broader driver. A new sibling is cleaner than overloading the existing principle.

**Tests:** S1–S4 pass. P1/P3 with "Find the pattern" pass — independent and additive (compression from experience satisfies this principle without satisfying the sibling). Distillation: no words to cut.

**Connection to PR #319:** The self-maintain scan that surfaced this principle was triggered by the same session that produced the "Find the pattern" scope clause rework. PR #319 removed the construction scope clause; PR #320 adds the principle that explains why the scope was too narrow — compression (the strand's quality signal) arrives through any mechanism, not only through construction-time pattern-seeking.

**Illustration (for Confluence explanation page):** Peter: "One way to observe better understanding in code is the absence of if statements." Each conditional is special-casing made visible — when the abstraction is right, the branches disappear. A concrete instance of "fewer parts, less special-casing."

**Conviction (S|L): high | high**
