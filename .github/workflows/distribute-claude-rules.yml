name: Distribute Claude Rules

on:
  schedule:
    - cron: '0 6 * * 1' # Every Monday at 06:00 UTC
  workflow_dispatch:

jobs:
  load-subscribers:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.build.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4

      - name: Discover repos and apply overrides
        id: build
        env:
          GH_TOKEN: ${{ secrets.REPOS_PAT }}
        run: |
          matrix=$(python3 -c "
          import yaml, json, subprocess

          with open('.github/claude-rules-overrides.yml') as f:
            config = yaml.safe_load(f) or {}
          overrides = config.get('overrides') or {}

          result = subprocess.run(
            ['gh', 'api', 'orgs/NXD-Solutions/repos', '--paginate', '--jq', '.[].full_name'],
            capture_output=True, text=True
          )
          repos = [r.strip() for r in result.stdout.strip().splitlines() if r.strip()]

          include = []
          for repo in repos:
            override = overrides.get(repo, {})
            if override.get('exclude'):
              continue
            role = override.get('role', 'Developer')
            include.append({'repo': repo, 'role': role})

          print(json.dumps({'include': include}))
          ")
          echo "matrix=$matrix" >> "$GITHUB_OUTPUT"

  sync:
    needs: load-subscribers
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{ fromJSON(needs.load-subscribers.outputs.matrix) }}
      fail-fast: false
    steps:
      - name: Checkout NXD-Solutions/.github (scripts)
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Checkout subscriber repo
        uses: actions/checkout@v4
        with:
          repository: ${{ matrix.repo }}
          token: ${{ secrets.REPOS_PAT }}
          path: subscriber

      - name: Run sync script
        working-directory: subscriber
        env:
          CONFLUENCE_EMAIL: ${{ secrets.CONFLUENCE_EMAIL }}
          CONFLUENCE_API_TOKEN: ${{ secrets.CONFLUENCE_API_TOKEN }}
        run: $GITHUB_WORKSPACE/node_modules/.bin/tsx $GITHUB_WORKSPACE/scripts/sync-decisions.ts --role ${{ matrix.role }}

      - name: Copy static Claude rules
        run: cp $GITHUB_WORKSPACE/static-claude-rules/* subscriber/.claude/rules/

      - name: Check for changes
        id: changes
        run: |
          if [ -z "$(git -C subscriber status --porcelain -- .claude/rules/)" ]; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "## ✅ Nothing new to sync — \`.claude/rules/\` is already up to date in \`${{ matrix.repo }}\`." >> "$GITHUB_STEP_SUMMARY"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Open PR if changed
        if: steps.changes.outputs.changed == 'true'
        id: open-pr
        uses: peter-evans/create-pull-request@v7
        with:
          token: ${{ secrets.REPOS_PAT }}
          path: subscriber
          branch: automated/update-ai-coding-rules
          base: main
          delete-branch: true
          add-paths: .claude/rules/
          commit-message: "Update AI coding rules from NXD Decision Log"
          title: "automated: Update AI coding rules from NXD Decision Log"
          body: |
            Auto-generated by the **Distribute Claude Rules** workflow in `NXD-Solutions/.github`.

            Updates `.claude/rules/decision-log-extract.md` from the `## AI Summary — ${{ matrix.role }}`
            sections on each NXD decision page in Confluence.

            Also syncs static Claude rules from `static-claude-rules/` in `NXD-Solutions/.github`.

            Review the diff to confirm all constraints look correct, then merge.
          labels: chore

      - name: Report PR to summary
        if: steps.open-pr.outputs.pull-request-url != ''
        run: |
          echo "## 🔀 PR opened for \`${{ matrix.repo }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "${{ steps.open-pr.outputs.pull-request-url }}" >> "$GITHUB_STEP_SUMMARY"